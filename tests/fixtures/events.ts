/**
 * Re-export of MOCK_EVENTS from lib/mocks/events.ts.
 * Kept here for backward compatibility with existing test imports.
 * New tests should import directly from "@/lib/mocks/events".
 */
export { MOCK_EVENTS } from "@/lib/mocks/events";
